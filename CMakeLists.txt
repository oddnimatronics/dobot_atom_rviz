cmake_minimum_required(VERSION 3.8)
project(dobot_atom_description)

find_package(ament_cmake REQUIRED)

install(
        DIRECTORY meshes xacro config urdf
        DESTINATION share/${PROJECT_NAME}/
)

install(
        DIRECTORY launch
        DESTINATION share/${PROJECT_NAME}/
)

ament_package()
